const express = require('express');
const db = require('../db');

const router = express.Router();

function findOrCreateRoom() {
  let roomCode
}

/* GET users listing. */
router.get('/', (req, res, next) => {
  res.send('skull lords only!');
});

module.exports = router;
